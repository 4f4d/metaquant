# Notebooks

Research notebooks — no `final_final2` naming; every experiment records
Git SHA, dataset version, config hash, model hash, windows (MQ-FR-013).

Naming: `E02_basis_baseline_2025-09-24.ipynb`
